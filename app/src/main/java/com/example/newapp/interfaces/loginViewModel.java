package com.example.newapp.interfaces;

import com.example.newapp.domain.models.loginUserData;
import com.example.newapp.global.Response;

public interface loginViewModel {
    void loginUser(loginUserData data);
}
